<?xml version="1.0" encoding="utf-8"?>
<Window
	x:Class="Magnesium.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="using:Magnesium"
	mc:Ignorable="d">

	<Window.SystemBackdrop>
		<MicaBackdrop />
	</Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Height="32" Grid.Column="0" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="TitleBarIcon" Source="ms-appx:///Assets/StoreLogo.png" Grid.Column="1" HorizontalAlignment="Left" Width="16" Height="16" Margin="8,0,0,0"/>
            <TextBlock x:Name="TitleBarTextBlock" 
		        Text="Magnesium" 
		        Style="{StaticResource CaptionTextBlockStyle}"
		        Grid.Column="1"
		        VerticalAlignment="Center"
		        Margin="28,0,0,0"/>
        </Grid>

        <Grid Margin="10,6,10,6" ColumnSpacing="10" Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" AllowDrop="True" DragEnter="FileListView_DragEnter" Drop="FileListView_Drop">
                <ListView x:Name="FileListView" ItemsSource="{x:Bind VideoFiles}" SelectionMode="None">
                    <ListView.HeaderTemplate>
                        <DataTemplate>
                            <Grid Padding="14" ColumnSpacing="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" MinWidth="250"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="File" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="Size" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="FPS" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="New size" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="Reduction" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="5" Text="Audio" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="6" Text="Status" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="7" Text="" FontWeight="SemiBold"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.HeaderTemplate>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:VideoFile">
                            <Border Padding="12,4" CornerRadius="{StaticResource OverlayCornerRadius}" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
				Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">
                                <Grid ColumnSpacing="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*" MinWidth="150"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Margin="0,0,6,0" CornerRadius="{StaticResource OverlayCornerRadius}" Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">
                                        <Image Source="{Binding ThumbnailPath}" Height="64" />
                                    </Border>

                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Grid.Column="2" Text="{Binding OriginalSize}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Fps}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="4" Text="{Binding NewSize}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="5" Text="{Binding SavedPercentage}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <TextBlock Grid.Column="6" Text="{Binding AudioTracks}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="7" Text="{Binding Status}" VerticalAlignment="Center"/>

                                    <Button Grid.Column="8" Click="DeleteButton_Click" Tag="{Binding FullPath}" FontFamily="Segoe MDL2 Assets" Content="&#xE74D;" Background="Transparent" BorderThickness="0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel x:Name="DropHereText" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Drop files here..." Opacity="0.7"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Column="1" Margin="12,0,12,0" Spacing="10">
                <TextBlock Text="Configuration" Style="{ThemeResource TitleTextBlockStyle}"/>

                <TextBlock Text="Codec"/>
                <ComboBox x:Name="CodecComboBox" SelectedIndex="3">
                    <ComboBoxItem Content="H.264 (libx264)"/>
                    <ComboBoxItem Content="H.265 (libx265)"/>
                    <ComboBoxItem Content="AV1 (libaom-av1)"/>
                    <ComboBoxItem Content="H.265 NVENC (hevc_nvenc)"/>
                </ComboBox>

                <TextBlock Text="Quality (CRF: 0-51, less is better)"/>
                <Slider x:Name="QualitySlider" Minimum="0" Maximum="51" Value="32" StepFrequency="1"/>

                <TextBlock Text="FPS"/>
                <ComboBox x:Name="FpsComboBox">
                    <ComboBoxItem Content="Same as input" Tag="0" IsSelected="True"/>
                    <ComboBoxItem Content="29.97" Tag="29.97"/>
                    <ComboBoxItem Content="30" Tag="30"/>
                    <ComboBoxItem Content="50" Tag="50"/>
                    <ComboBoxItem Content="59.94" Tag="59.94"/>
                    <ComboBoxItem Content="60" Tag="60"/>
                </ComboBox>

                <TextBlock Text="Output folders"/>
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="OutputFolderTextBox" IsReadOnly="True" Text="Same as input"/>
                    <Button Grid.Column="1" Content="Select..." Click="SelectFolderButton_Click"/>
                </Grid>

                <Button x:Name="StartButton" Content="Convert" HorizontalAlignment="Stretch" Margin="0,20,0,0" Style="{ThemeResource AccentButtonStyle}" Click="StartButton_Click"/>
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Spacing="5" Padding="8,12">
                <TextBlock Text="Current file:" />
                <ProgressBar x:Name="CurrentFileProgressBar" Minimum="0" Maximum="100" />
                <TextBlock Text="Total:" />
                <ProgressBar x:Name="TotalProgressBar" Minimum="0" Maximum="100"/>
            </StackPanel>


        </Grid>
    </Grid>

</Window>